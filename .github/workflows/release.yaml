name: release

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  version2: 1.0.${GITHUB_RUN_NUMBER}

jobs:
    build:
        name: build_release
        runs-on: ubuntu-latest 
        steps:
            - name: version
              run: |
                echo "Run: ${{ env.GITHUB_RUN_NUMBER }}"
